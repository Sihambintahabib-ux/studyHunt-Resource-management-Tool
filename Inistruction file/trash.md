
JWT_SECRET=studynest_super_secret_key_min_32_chars_long
--- 
- jwt token
- [uploadedBy: Schema.Types.ObjectId | string;](https://mongoosejs.com/docs/typescript/schemas.html)
-

---

62UzyQ4BqwpBDOtp

studynest

mongodb+srv://studynest:62UzyQ4BqwpBDOtp@cluster0.hohlyvu.mongodb.net/?appName=Cluster0

---

echo "# studyHunt-Resource-management-Tool" >> README.md
git init
git add .
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/Sihambintahabib-ux/studyHunt-Resource-management-Tool.git
git push -u origin main

- Force push the changes: If you need to overwrite the remote branch history (which should be used with caution), you can force push your changes to the remote repository.
  `git push origin main --force`
  Caution: Using --force can overwrite changes made by others. It is generally recommended to use git push without the force flag unless you are certain it is necessary and safe to do so.

---

git status
git add .
git commit -m "Describe what you changed"
git push
git pull

---

3. Essential "Day Two" Commands
   Command Purpose
   git log --oneline View a simplified list of your past commits.
   git diff See the exact lines of code you changed since the last commit.
   git checkout -b <name> Create and switch to a new branch for a specific feature.
   git stash Temporarily "hide" your current uncommitted changes to work on something else.
   git restore <file> Undo uncommitted changes in a specific file and reset it to the last commit.

---

| Command                | Purpose                                                                        |
| ---------------------- | ------------------------------------------------------------------------------ |
| git log --oneline      | View a simplified list of your past commits.                                   |
| git diff               | See the exact lines of code you changed since the last commit.                 |
| git checkout -b <name> | Create and switch to a new branch for a specific feature.                      |
| git stash              | Temporarily "hide" your current uncommitted changes to work on something else. |
| git restore <file>     | Undo uncommitted changes in a specific file and reset it to the last commit.   |

---

studyHunt-Resource-management-Tool

studyHunt - A platform where students upload, discover, and organise study materials — notes, PDFs, video links, cheat sheets — grouped by subject and topic. Users rate and review resources. AI summarises reviews, suggests related content, and powers a study chatbot.

---

Force push the changes: If you need to overwrite the remote branch history (which should be used with caution), you can force push your changes to the remote repository.
git push origin main --force (replace main with your branch name if different)
Caution: Using --force can overwrite changes made by others. It is generally recommended to use git push without the force flag unless you are certain it is necessary and safe to do so.
